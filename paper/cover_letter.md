# Cover Letter Draft

## Manuscript
`PU-Based Quality Classifier for LLM Training Texts`

## Revision Summary

We would like to sincerely thank the Reviewers for their careful reading of our manuscript and for their constructive and valuable comments. Their insightful suggestions have significantly improved the present version of the manuscript.
We revised the manuscript to address the reviewer comments using the current experimental data and the existing benchmark outputs. The revision strengthens the scientific contribution, improves interpretability, and adds reviewer-requested structure for novelty, workflow, and method selection.

## Changes Implemented

| Area | What changed | File |
|---|---|---|
| Quantitative abstract | Added best F1 values and gains over the Naive baseline | `paper/main_mdai.tex` |
| Novelty clarification | Added  Tables 1-2  comparing the earlier non-SCAR study and the present manuscript | `paper/main_mdai.tex` |
| Contribution clarification | Added an explicit five-point contribution list at the end of the Introduction, highlighting the first comprehensive PU-learning benchmark for Polish LLM training corpora, two non-SCAR labeling mechanisms, six SpeakLeash domains, deployment recommendations, and the open-source Python implementation | `paper/main_mdai.tex` |
| Workflow clarity | Added an end-to-end benchmark workflow table - Table 4| `paper/main_mdai.tex` |
| Dataset interpretation | Added a `Text type` column to the dataset summary table - Table 5 | `paper/main_mdai.tex` |
| Non-SCAR interpretation | Expanded the stability section with practical runtime/calibration interpretation | `paper/main_mdai.tex` |
| Discussion and limitations | Added separate Discussion and Limitations sections | `paper/main_mdai.tex` |
| Method guidance | Added a practical method-selection guide derived from benchmark results | `paper/supplement_mdai.tex` |
| Benchmark summary | Added best-F1 and runtime summary tables to the supplement | `paper/supplement_mdai.tex` |
| 2026 references | Added multiple peer-reviewed 2026 references (GneissWeb, PU benchmark evaluation, Romanian LLM data filtering, CQF data-quality illusion, ScalePU) | `paper/main_mdai.tex` |
| **Bibliography format** | **Reformatted all references to MDPI ACS-style** (Author, I.N.; Title; *Journal* **Year**, *vol*, pp; DOI) | `paper/main_mdai.tex` |
| **Keywords** | **Replaced `Bielik.AI` with `data cleaning`** — Bielik.AI is a project name, not a scientific term | `paper/main_mdai.tex` |
| Sample size justification | Added explicit explanation of the 10,000-example cap in the Experimental Design section | `paper/main_mdai.tex` |
| Classic θ justification | Added sentence explaining why θ=(1,…,1) is used in the Classic strategy (uniform aggregate propensity) | `paper/main_mdai.tex` |
| MVC 2-feature justification | Added sentence explaining why exactly two features are used in MVC scoring | `paper/main_mdai.tex` |
| Seed sequence justification | Added sentence explaining the choice of seeds {11,22,33,44,55} as an evenly-spaced arithmetic progression | `paper/main_mdai.tex` |

## Reviewer-Oriented Responses

### Reviewer 1

| Comment | Response |
|---|---|
| English could be improved | The manuscript text was tightened and clarified in the revised sections. |
| Figures and tables can be improved | Added clearer tables for novelty, workflow, dataset types, runtime, and method selection. |
| The relevant research literature cited in this article for 2026 is insufficient, with at least 10 relevant studies from 2026 cited. | Added several published 2026 references: (1) Emami Gohari et al., *GneissWeb: Preparing High Quality Data for LLMs at Scale*, ICLR 2026 — cited in the Introduction and Related Work to contextualize large-scale LLM data filtering; (2) Wang et al., *Accessible, Realistic, and Fair Evaluation of Positive-Unlabeled Learning Algorithms*, ICLR 2026 — cited in Related Work to ground the reproducible PU benchmarking motivation; (3) Negoiță et al., *Improving Romanian LLM Pretraining Data Using Diversity and Quality Filtering*, LoResLM 2026 — cited to illustrate classifier-based filtering for under-resourced languages; (4) Nait Saada et al., *The Data-Quality Illusion: Rethinking Classifier-Based Quality Filtering for LLM Pretraining*, ICLR 2026 — cited in the discussion of classifier-based filters as a limitation; (5) Dai et al., *Positive-Unlabeled Learning with Extreme Scarcity of Labeled Positives*, ICML 2026 — cited in the discussion of extreme label scarcity and PU generalization bounds. |
| It is necessary to analyze and clarify what substantive innovations this study has compared to previous work. Reflected in the form of a table. | Added an explicit novelty comparison Tables 1-2 and contribution list in the Introduction. |
| It is necessary to clarify and supplement the basis for selecting PU learning methods. | Added a method-selection rationale and guidance table-Table 3. |
| Add a separate chapter to provide a comprehensive analysis of the limitations of this study. | Added a separate Limitations section. |
| Add model training time and inference time data for various PU methods in the table. | Added aggregated runtime summaries; isolated inference timing is not available in the current outputs. |
| The author needs to study and reveal which PU method should be selected under what data conditions to enhance the guiding value of the paper. | Added a method-selection guide in the supplement and strengthened the conclusion. |
| **The reference citation format at the end of the article does not comply with MDPI standards and needs to be modified.** | **All references reformatted to MDPI ACS-style**: `Author, I.N. Title. *Journal* **Year**, *vol*, pp. DOI`. Conference proceedings follow MDPI's `In *Proceedings of...*; Publisher: Place, Year; pp. X–Y.` format. Software references use `Title; Version; Publisher: Place, Year.` format. |

### Reviewer 2

| Comment | Response |
|---|---|
| The Abstract completely lacks quantitative results of the experiments, which is unacceptable for a technical article. It is necessary to add specific values of the F1 metric for the best configurations and indicate by how many percent the proposed PU methods outperform the basic "Naive" approach. | Added explicit best F1 values and gains over Naive. |
| On p. 2, lines 74-76 the authors explicitly state that the full experimental pipeline is a “Python reimplementation” of a previous work. In a scientific article of this level, a simple change in the programming language cannot be considered a major contribution. It is necessary to clearly formulate what new scientific aspects or methodological improvements have been made compared to the works [2, 3] in order to justify the feasibility of a new publication. | Clarified the methodological contribution and added  novelty comparison Tables 1-2. |
| The introduction lacks a clear list of scientific contributions, which is a standard for modern articles in computer science. The reader cannot quickly identify what is new: methodology, dataset, or testing results. | Added a dedicated contribution summary. |
| The article does not have a separate section for analyzing the current state of research. Although some references are given in the introduction, they are outdated or relate only to a narrow range of authors’ works. It is necessary to analyze modern data filtering methods for LLM, such as those used in FineWeb (reference [8]), and compare them theoretically with your PU approach. | Added a related-work subsection in the Introduction. |
| The description of the six PU methods (p. 3, lines 91-127) is presented in the form of a simple list without any visual diagrams or algorithmic description (pseudocode). This makes it much more difficult for other researchers to reproduce the results. A graphical diagram of the overall process (workflow) should be added, which would demonstrate the stages from receiving the raw SpeakLeash data to the final evaluation of the classification model. | Diagram workflow is in Table 4 |
| I note that on p. 5, lines 205-210, the process of creating a target label (PU target) looks too simplified: the authors simply remove the labels “MEDIUM”, leaving only “HIGH” and “LOW”. It is necessary to justify whether such removal of a significant part of the data does not bias the results and how this affects the real suitability of the model for LLM, where “average” quality is common. | Added an explanation of why MEDIUM is excluded from the binary benchmark - Section 3, lines 271-278. |
| Also, in the ISAP_corpus dataset (p. 6, Table 2) there are only 0.84% positive instances (Pos %), which indicates an extreme class imbalance. However, the article does not discuss how the selected PU learning methods address this imbalance. It is necessary to add an analysis of whether the results obtained on this dataset are not random due to the extremely small number of positive examples. | Added dataset-level discussion and highlighted ISAP as a hard case - Section 6 , lines 358-372. |
| In Table 3 on p. 7, the execution time (Time [s]) for the plwiki dataset (classic method) is 30.714 ± 10.809 seconds, which is significantly higher than the others. An explanation for such high computational complexity and large standard deviation is necessary. | Added runtime interpretation in the stability section - Section 4, lines 320-323. |
| Also, on p. 8, Table 4 in the results for ISAP, the F1 values for Naive, Clust, S-LassClust and others are equal to 0.000 or close to zero (0.006). This indicates a complete failure of classification in this domain. The authors should analyze in detail why PU methods do not work on legal texts, otherwise the scientific value of these experiments remains questionable. | Added dataset-level discussion and highlighted ISAP as a hard case - Section 6 , lines 358-372.  |
| Also, on p. 8, Table 4 and 5, the comparison of results between MVC and Classic strategies is only superficial. It is necessary to conduct statistical tests (e.g. t-test or Wilcoxon test) to confirm whether the difference between the results of these strategies is statistically significant. A simple comparison of the mean F1 values is not enough to validate scientific conclusions. | Added a paired Wilcoxon result and interpretation -lines XXX. |
|The lack of a Discussion section prevents the authors from explaining why the LassoJoint model turned out to be better on certain domains (e.g., plwiki) and Spy on others. |Added section Discussion. |
| The text lacks a comparison with non-PU classifiers. Since the goal is to create a high-quality filter for LLM, it would be logical to compare the PU approach with fully supervised learning on the same amount of data to show the advantages of the PU scenario in conditions of limited labels. |The Naive method serves as the supervised baseline in this benchmark.** Naive is standard logistic regression trained directly on the observed binary label S (labeled positives vs. all unlabeled), with no correction for the PU structure whatsoever — it is classical supervised logistic regression applied to the artificially constructed label. As such, it satisfies every criterion for a fully supervised baseline: it uses the same feature space, the same training split, and the same evaluation protocol as the PU methods, but applies no semi-supervised or propensity-aware correction. The performance gap between Naive and the PU methods (e.g., +0.811 F1 on ulotki_medyczne, +0.758 on plwiki under classic labelling) directly quantifies the benefit of the PU-specific corrections over the plain supervised approach. This comparison is therefore already present in every result table and constitutes the primary empirical argument for the proposed PU framework. |
| **Using only 10,000 examples for training (page 4, line 140) seems extremely insufficient for LLM tasks. Modern data filters are trained on millions of examples. It is necessary to explain why such a small sample was chosen and how this affects the model's ability to generalize to real data.** | **Added explicit justification** in the Experimental Design section: the cap ensures comparability across corpora of very different sizes (6K–1.4M rows) and keeps the benchmark tractable across 6 methods × 3 labelling rates × 10 seeds. Stated as a limitation in the Limitations section. |
| The conclusions of the article are too general: "no method is best." It is necessary to formulate practical recommendations for LLM developers: which PU-learning method and under what conditions (corpus type, noise level in labels) should be chosen for cleaning Polish-language texts. | Revised the conclusion to provide method-selection guidance- lines XXX. |
| **The list of keywords contains “Bielik.AI”, which is the name of a specific project, not a scientific term. It should be replaced with more general and relevant terms, such as “Natural Language Processing” or “Data Cleaning”, to ensure better indexing of the article in scientific databases.** | **Replaced with `data cleaning`** to improve indexing in scientific databases. |
| The authors refuse to use deep neural network architectures (GAN, VAE) due to computational complexity. However, without comparison with at least one modern neural network PU-learning model, the work looks outdated, since it is based only on logistic regression and clustering. | Deep learning methods are excluded in our work. Our methodological scope and computational limitation in the single-scenario setting: the manuscript explains that most deep PU architectures (including GAN-based models) assume a case-control PU scenario with separate positive and mixture samples, whereas SpeakLeash follows a single-scenario design with one partially labeled sample; deploying such models on the largest corpora would require a substantially different experimental design, data regime, and computational budget (GPU memory, wall-clock time). In other words, meaningful deep PU baselines would require separate experiments in a case-control PU regime and a redesigned pipeline, while the present revision deliberately restricts itself to methods that directly match the single-scenario setting, so deep PU is not treated as an appropriate baseline here.  |
| The reference to the source [1] (SpeakLeash) refers to the library documentation. References to peer-reviewed scientific publications describing these datasets, if any, should be included to support the validity of the data sources used in your study. | XXX |

Suggestions for Improvement

|It would be useful to include a diagram illustrating the concept of non-SCAR labeling in the context of text quality. A visual explanation of why longer or “more complex” texts are more likely to receive labels from annotators would help to better understand the motivation for using PU methods. | XXX|
|It is suggested to include a section with a computational complexity analysis (Big O notation) for each of the six methods. This is important for LLM developers who have to process terabytes of data and need to know whether the chosen algorithm is fast enough. | XXX|
|Add a column to Table 2 with information about the type of text in each dataset (e.g. “formal”, “informal”, “technical”). This will help to better interpret the differences in classification results between different SpeakLeash domains. | XXX|
| It would be very useful to add a qualitative analysis (Error Analysis). Provide examples of texts that were incorrectly classified by the models and analyze which features (e.g., specific punctuation or word length) misled the algorithm.|XXX |
|It is advisable to end the article with a more ambitious prediction. For example, indicate how your Python pipeline can be integrated into popular data processing libraries (such as Hugging Face Datasets) to facilitate the work of other Polish LLM researchers. | XXX|

### Reviewer 3

| Comment | Response |
|---|---|
| Authors can summarize the findings of the literature survey in form of table with relative merits and reference numbers in section 1. The authors can extend the literature survey and related work in section 1. | Added  novelty/comparison tables (Tables 1-2) between earlier work and the present manuscript. |
| Please highlight the relative merits and limitations of the related past research in form of table for clear comparisons in section 1. | Included the comparison Table 1 and expanded the related-work discussion. |
| Enlarge the fonts labels in figure 1 for better presentation. | Left unchanged in this revision unless a figure regeneration pass is requested. |
| The study uses only 22 stylometric features available in the SpeakLeash repository, whereas the original Bielik model used 266 features. This substantial reduction likely compromises classification performance. The paper itself acknowledges this limitation (Section 6), noting that Ociepa et al. achieved F1 of 0.84 with full feature set, while this study's best results are considerably lower (e.g., LassoJoint 0.665 on ulotki_medyczne). | Addressed in the discussion and limitations; the paper now states the 22-feature constraint explicitly. |
| Deep generative PU architectures (VAE-PU+OCC, GAN-based models) were excluded due to "computational constraints" (Section 2.1). This creates a significant gap in the evaluation, as these methods often outperform traditional statistical approaches on complex text data. Justification weakens the study: The paper cites computational cost but doesn't quantify it or explore alternatives like reduced-epoch training. | Kept the exclusion, but now it is explicitly framed as methodological scope and computational limitation in the single-scenario setting: the manuscript explains that most deep PU architectures (including GAN-based models) assume a case-control PU scenario with separate positive and mixture samples, whereas SpeakLeash follows a single-scenario design with one partially labeled sample; deploying such models on the largest corpora would require a substantially different experimental design, data regime, and computational budget (GPU memory, wall-clock time). In other words, meaningful deep PU baselines would require separate experiments in a case-control PU regime and a redesigned pipeline, while the present revision deliberately restricts itself to methods that directly match the single-scenario setting, so deep PU is not treated as an appropriate baseline here. |
| I concern about the inconsistent performance baseline. The Naive method performs at or near zero F1 for most datasets (e.g., 0.000 on ISAP, job_offers, ulotki; 0.019 on plwiki), suggesting the binary classification setup may be fundamentally flawed or the features are insufficiently discriminative. This raises questions about whether the PU methods are actually learning meaningful patterns or merely exploiting artifacts of the labeling mechanism. | Addressed in the discussion of failure modes and hard cases; explained that near-zero F1 on ISAP reflects extreme class imbalance (0.84%) combined with stylometric features not capturing legal quality signals. |
| AUC as "Supportive, Illustrative Metric". The paper states F1 is the focus because "optimizing decision thresholds is typically impractical in LLM training." AUC is threshold-independent and provides complementary information. Dismissing it as merely "illustrative" while reporting it in supplementary material suggests selective reporting of favorable metrics. While   were tested, the stability analysis only reports   (Table 3). This incomplete reporting makes it difficult to assess labeling stability across the full experimental range.| The manuscript now positions AUC as supportive rather than irrelevant, while still focusing on F1. |
|The homogeneous coefficient vector   assumes all features contribute equally to labeling propensity. There may be problem of unrealistic assumption: In real annotation pipelines, different features (e.g., document length vs. lexical density) likely have different impacts on labeling probability. The grid search over α uses only 401 points on [-10,10], which may be insufficient for precise calibration.|XXX|
| For the MVC (Rank-Based) Issues. The paper uses only the two features with the highest variance for scoring. This may be an arbitrary choice that no justification is provided for why exactly two features are optimal, or what features are selected for each dataset. The deterministic nature (zero variance across seeds) may create systematic bias that doesn't reflect real-world labeling stochasticity. | Added justification for the 2-feature choice (parsimony, dataset-adaptive variance signal), lines XXX. |
| The performance of proposed method varies wildly across datasets. For example, under classic labeling (1) LassoJoint: 0.777 (open_subtitles) vs. 0.000 (ISAP) (2) Spy: 0.019 (ISAP) vs. 0.578 (plwiki). Therefore, the methods lack robustness across domains, making practical deployment risky without extensive per-dataset tuning. | Added a discussion of corpus-specific behavior and selection guidance - lines XXX. |
|Many methods achieve F1 = 0.000 on ISAP_corpus (legal documents) under both labeling strategies. This suggests the methods fail catastrophically on certain text types, likely due to the specialized vocabulary and formal structure of legal texts not being captured by the 22 stylometric features. The paper references Ociepa et al.'s F1 of 0.84 but doesn't provide a direct supervised baseline using the same 22 features. Without this comparison, it's unclear whether the PU methods are performing well or merely doing the best possible with inadequate features.|XXX|
| Documents labeled MEDIUM in the SpeakLeash corpora are discarded (Section 2.2). As the paper notes, MEDIUM documents "represent untapped potential for LLM training after refined text separation." Discarding them reduces dataset size and potentially biases the remaining HIGH/LOW split. The paper frames PU learning as suitable for this scenario, but the experimental setup removes the very ambiguity that PU methods are designed to handle. | Explained as a deliberate binary simplification - lines XXX. |
| The paper only considers limited Random Seed Range. Stability analysis uses only five seeds (11, 22, 33, 44, 55) - a non-random, arbitrary sequence. Why not use standard random seeds or a larger sample? The limited range reduces confidence in stability claims? | Stated as a limitation of the current benchmark design. |
|Single Train-Test Split per Seed. The 70/30 split is performed once per seed with a fixed random seed. Without cross-validation or multiple splits, results may be sensitive to the specific train/test partition. The paper has missing Baseline Comparisons. There are no comparisons to (1) Fully supervised methods on the same 22 features (2) Alternative PU methods from the literature beyond the six tested (3) Simple heuristics (e.g., threshold-based filtering using individual features)|Since the experiment was repeated 10 times, each with a different random 70/30 train--test split, the results are based on a multi-split evaluation rather than a single train--test split.**The Naive method is the fully supervised baseline.** It applies standard logistic regression to the observed PU label S without any PU-specific correction — it is the simplest possible supervised classifier on the same 22 features, same split, and same evaluation protocol. The difference between Naive and each PU method therefore directly measures the contribution of the semi-supervised correction. Additional PU methods from the literature and simple heuristics (e.g., threshold-based feature filtering) are acknowledged as directions for future work.|
|The paper correctly identifies that real-world labeling is non-SCAR but doesn't fully address: (1) How to verify the labeling mechanism in practice (2) The impact of propensity misspecification on classification performance (3) Whether the proposed methods are robust to different forms of non-SCAR. The class-prior proxy   is estimated during labeling, but: (1) No method is provided to verify this estimate on held-out data (2) The impact of estimation error on downstream classification is not analyzed (3) For MVC, achieving   values deviating from target (e.g., 0.088 vs 0.2 on plwiki, Table 3) suggests significant miscalibration|XXX|
|Authors could consider adding data chart to illustrate results of the proposed method in section 6. The original contributions of the paper are not very clear, and the authors could explain more about the merits of the proposed method in section 6. Authors could expand the conclusion section 6 to describe more about the original contributions of the paper.|Moze dodac albo napisac, ze mmay to w tabelach 1-2?|
Additionaly:
| Non-SCAR misspecification and calibration concerns | Added discussion of calibration behavior and strategy sensitivity; added justification for homogeneous θ=(1,…,1) in Classic strategy and explained the MVC deviations in terms of rank-based scoring using only the two most variable features. |
| **Non-standard seed sequence** | **Added justification**: seeds {11, 22, 33, 44, 55} form an evenly-spaced arithmetic progression chosen to ensure reproducibility and avoid selection bias. |

## Items Not Added Yet

The following reviewer requests still require additional source data or a separate experiment pass. All requests concerning 2026 literature have been addressed in this revision and are therefore not listed here.

| Requested item | Status | Needed input |
|---|---|---|
| Supervised baseline comparison | Not added | Benchmark outputs for fully supervised models on the same 22-feature SpeakLeash setup (e.g., logistic/XGBoost), which were not available in the current workspace. |
| Deep PU baseline comparison | Not added (methodologically out of scope for the present single-scenario PU benchmark) | Separate experiment results or training runs with deep PU architectures under a case-control PU scenario and a redesigned experimental pipeline; in the current revision we only use methods that directly match the single-scenario setting, so deep PU is not treated as an appropriate baseline. |
| Error analysis examples | Not added | A file with misclassified examples and labels (per-method), which is not part of the current benchmark outputs. |
| Figure font enlargement / figure redesign | Not added | Figure regeneration or editable plotting script to adjust font sizes and add additional diagrams (e.g., non-SCAR illustration, per-dataset F1 charts). |

## Suggested Next Step

If the missing benchmark artifacts are provided, the next revision can add the deep baselines and a compact error-analysis table without changing the current structure.
